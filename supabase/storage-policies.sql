-- ============================================================
-- Gazameel — سياسات Storage (اختياري / للتوضيح)
-- ============================================================
-- التطبيق يرفع وينزّل عبر service_role من الـ API فقط
-- (انظر src/app/api/upload و src/lib/moderation.ts).
-- service_role يتجاوز RLS على storage.objects — لذلك المرحلة 1
-- تعمل بدون سياسات إضافية طالما الـ bucket اسمه resources و Private.
--
-- نفّذ هذا الملف فقط إذا أردت منع أي وصول من anon/authenticated
-- مباشرةً إلى الكائنات (دفاع إضافي). لا حاجة له إن اكتفيت بـ Private.
-- ============================================================

-- ملاحظة: لا نضيف سياسة SELECT للعامة على الملفات.
-- الطلاب يحصلون على رابط موقّت من السيرفر (Signed URL).

-- إن ظهرت أخطاء صلاحيات من العميل لاحقًا، راجع أن الرفع لا يحدث
-- من المتصفح مباشرة بل من Route Handlers فقط.
