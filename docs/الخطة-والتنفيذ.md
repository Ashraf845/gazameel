# الخطة والتنفيذ — Gazameel

تم تنفيذ المراحل 1–3 في الكود تحت `/Users/ashraf/Projects/gazameel`.
ما يتبقى عليك: لصق المفاتيح والأسرار الحقيقية (لا تُخترع في المستودع).

## الدروس (اقرأ بالترتيب)

1. [درس-المرحلة-1.md](./درس-المرحلة-1.md) — Supabase + Google + Storage + Hub/Moderation
2. [درس-المرحلة-2.md](./درس-المرحلة-2.md) — تيليجرام + كويز + Cron
3. [درس-المرحلة-3.md](./درس-المرحلة-3.md) — مساهمون + استطلاعات + واتساب

## ما تم بناؤه

### المرحلة 1
- Google OAuth + onboarding (رقم جامعي + مواد)
- `/upload` MIME / 15MB / حد 3 pending + Storage خاص
- `/admin` طابور من DB + رفع مباشر + مواعيد
- `/hub` + Signed URL؛ `/my-submissions`؛ Feed من `updates_feed`
- حماية `is_admin` في RLS + نقل الملف إلى `approved/` عند الموافقة
- سلوك ودّي بدون مفاتيح + `.env.local` بـ placeholders

### المرحلة 2
- Webhook تيليجرام + فحص `ADMIN_TELEGRAM_CHAT_ID` + idempotency
- `/daily` بأزرار بدون كشف الجواب مسبقًا؛ `/countdown`
- أسئلة أدمن + CSV؛ `/quiz`؛ `/progress`
- Cron تذكيرات محمي بـ `CRON_SECRET`؛ آمن بدون توكن

### المرحلة 3
- `/polls` + إنشاء من الأدمن
- `/contributors` مع Banner عند غياب الإعداد
- واتساب في `/about` **والتذييل**
- رابط `/telegram` في النافبار

## تشغيل

انظر [README.md](../README.md) — Node 20+ وملء `.env.local` من `.env.example`.
